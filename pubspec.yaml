name: backend_daszx_inventory
description: Reusable backend template with Dart, Shelf and PostgreSQL.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.1

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  dart_jsonwebtoken: ^3.3.1
  dbcrypt: ^2.0.0
  postgres: ^3.5.9

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
